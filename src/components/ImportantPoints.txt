1) remember when state change in out application that components rerender (means return( which is showing content on screen ) rerender) 
but useEffects, functions won't run again untill call inside return() as we discussed only return() rerenders.